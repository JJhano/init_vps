# =============================================================================
# Configuración de VPS - Variables de entorno
# =============================================================================
# Copia este archivo como .env y modifica los valores según tu necesidad
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# SSH
# -----------------------------------------------------------------------------
# Puerto SSH personalizado (por defecto: 22)
SSH_PORT=22

# Usuario administrador para SSH (si no es root)
SSH_ADMIN_USER=admin

# Llaves públicas SSH de administradores
# Opción 1: Crear archivo ssh_admin_keys.txt con una llave por línea (RECOMENDADO)
# Opción 2: Definir una sola llave aquí
# Obtén tu llave con: cat ~/.ssh/id_rsa.pub
SSH_ADMIN_KEYS="ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC... tu-email@example.com"

# Llave pública SSH de GitHub Actions (para deployments)
# Genera una específica para CI/CD
SSH_GITHUB_KEY="ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC... github-actions"

# Deshabilitar autenticación por contraseña (yes/no)
# ADVERTENCIA: Solo configurar en 'yes' si has añadido tus llaves SSH
SSH_DISABLE_PASSWORD=no

# Deshabilitar login directo de root (yes/no)
SSH_DISABLE_ROOT=no

# -----------------------------------------------------------------------------
# Fail2ban
# -----------------------------------------------------------------------------
# IPs que nunca serán baneadas (separadas por espacios)
# Incluye tu IP de casa/oficina para no quedarte fuera
FAIL2BAN_WHITELIST="127.0.0.1/8 ::1"

# Intentos máximos antes de banear
FAIL2BAN_MAXRETRY=3

# Tiempo de baneo para SSH (ejemplos: 10m, 1h, 1d)
FAIL2BAN_BANTIME=1h

# -----------------------------------------------------------------------------
# ISPConfig
# -----------------------------------------------------------------------------
# Hostname del servidor (debe ser un FQDN válido)
ISPCONFIG_HOSTNAME=server1.example.com

# Servidor web (apache o nginx)
ISPCONFIG_USE_WEBSERVER=apache

# Versiones de PHP a instalar (php o php,php7.4,php8.0)
ISPCONFIG_USE_PHP=php

# Servidor FTP (pureftpd o proftpd)
ISPCONFIG_USE_FTPSERVER=pureftpd

# Contraseña root de MySQL (dejar vacío para modo interactivo)
ISPCONFIG_MYSQL_ROOT_PASSWORD=

# Contraseña admin de ISPConfig (dejar vacío para modo interactivo)
ISPCONFIG_ADMIN_PASSWORD=

# Datos para certificado SSL
ISPCONFIG_SSL_CERT_COUNTRY=US
ISPCONFIG_SSL_CERT_STATE=California
ISPCONFIG_SSL_CERT_LOCALITY=Los Angeles
ISPCONFIG_SSL_CERT_ORGANIZATION=MyCompany

# -----------------------------------------------------------------------------
# Coolify
# -----------------------------------------------------------------------------
# Dominio para acceder a Coolify (opcional)
COOLIFY_DOMAIN=

# Email para notificaciones (opcional)
COOLIFY_EMAIL=

# -----------------------------------------------------------------------------
# Mailcow
# -----------------------------------------------------------------------------
# Hostname completo para el servidor de email (FQDN obligatorio)
# Ejemplo: mail.tudominio.com
MAILCOW_HOSTNAME=mail.example.com

# Zona horaria
# Lista: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
MAILCOW_TIMEZONE=America/New_York

# Puertos HTTP/HTTPS para la interfaz web (solo si usas puertos alternativos)
# Por defecto: 80/443
MAILCOW_HTTP_PORT=80
MAILCOW_HTTPS_PORT=443

# Usar puertos alternativos automáticamente si hay conflicto con Coolify
# Valores: auto, yes, no
# - auto: Detecta Coolify y pregunta qué hacer
# - yes: Siempre usar puertos alternativos (8080/8443)
# - no: Siempre usar puertos estándar (80/443)
MAILCOW_USE_ALTERNATE_PORTS=auto

# -----------------------------------------------------------------------------
# Netdata
# -----------------------------------------------------------------------------
# Puerto para la interfaz web de Netdata
NETDATA_PORT=19999

# Netdata Cloud (opcional - para acceso remoto autenticado y multi-servidor)
# Obtén estos valores en: https://app.netdata.cloud
# Token de reclamación (claim token)
NETDATA_CLAIM_TOKEN=

# ID de la sala (room ID)
NETDATA_CLAIM_ROOMS=

# URL de Netdata Cloud (normalmente no es necesario cambiar)
NETDATA_CLAIM_URL=https://app.netdata.cloud
