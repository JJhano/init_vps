# =============================================================================
# Configuración Básica de VPS - Variables de entorno
# Ubuntu 24.04
# =============================================================================
# Para usar este archivo:
#   1. Copia este archivo: cp .env.basic.example .env
#   2. Edita .env y completa los valores marcados con TODO
#   3. Ejecuta: sudo ./run_basic.sh
# =============================================================================

# -----------------------------------------------------------------------------
# SSH - Configuración de acceso seguro
# -----------------------------------------------------------------------------

# Puerto SSH (22 es el estándar, cambia por seguridad)
# Recomendado: Usar un puerto no estándar entre 1024-65535
SSH_PORT=22

# Usuario administrador para SSH (si no quieres usar root directamente)
# Déjalo vacío si solo usarás root
SSH_ADMIN_USER=admin

# ⚠️ TODO: Agrega tu llave SSH pública
# Opción 1 (recomendada): Edita ssh_admin_keys.txt y agrega tus llaves (una por línea)
# Opción 2: Descomenta y pega tu llave aquí entre comillas
# SSH_ADMIN_KEYS="ssh-rsa AAAAB3NzaC1yc2EAAA... tu-email@example.com"
SSH_ADMIN_KEYS=""

# Llave SSH para GitHub Actions/CI-CD (opcional)
# Solo necesario si usas automatización
SSH_GITHUB_KEY=""

# Deshabilitar autenticación por contraseña
# ⚠️ IMPORTANTE: Déjalo en 'no' hasta verificar que funciona la llave SSH
# Después de probar que puedes conectarte con tu llave, cámbialo a 'yes'
SSH_DISABLE_PASSWORD=no

# Deshabilitar login directo de root
# ⚠️ IMPORTANTE: Solo activa esto si creaste un usuario administrador
SSH_DISABLE_ROOT=no

# -----------------------------------------------------------------------------
# Fail2ban - Protección contra ataques de fuerza bruta
# -----------------------------------------------------------------------------

# ⚠️ TODO: Agrega tu IP actual para no quedarte bloqueado
# Formato: "127.0.0.1/8 ::1 TU_IP_PUBLICA"
# Ejemplo: "127.0.0.1/8 ::1 203.0.113.100"
# Para obtener tu IP: curl ifconfig.me
FAIL2BAN_WHITELIST="127.0.0.1/8 ::1"

# Intentos máximos antes de banear una IP
FAIL2BAN_MAXRETRY=3

# Tiempo de baneo (ejemplos: 10m, 1h, 1d, 7d)
FAIL2BAN_BANTIME=1h

# -----------------------------------------------------------------------------
# UFW - Firewall
# -----------------------------------------------------------------------------
# No requiere configuración adicional
# El script 03_ufw.sh configurará automáticamente:
#   - Puerto SSH (el que definas en SSH_PORT)
#   - Puertos HTTP (80) y HTTPS (443)

# -----------------------------------------------------------------------------
# Docker (opcional)
# -----------------------------------------------------------------------------
# No requiere configuración adicional
# El script 05_docker.sh se ejecutará solo si aceptas al final
